+---المصدر (src)
    +---الأصول (assets)
    |   +---الصور (images)      
    |   +---الجافاسكريبت (js)      
    |   +---الأنماط (styles)         
    +---اللغات (locales)
    |       العربية (ar.json)
    |       الإنجليزية (en.json)
    +---العرض (views)
        +---المكونات (components)
        |   +---التذييل (footer)
        |   +---الترويسة (header)
        |   +---الرئيسية (home)
        |   +---المنتج (product)
        |   التعليقات (comments.twig)
        +---التخطيطات (layouts)
        |       الرئيسي (master.twig)
        +---الصفحات (pages)
            |   السلة (cart.twig)
            |   الرئيسية (index.twig)
            |   نقاط الولاء (loyalty.twig)
            |   صفحة-مفردة (page-single.twig)
            |   شكراً-لك (thank-you.twig)
            +---المدونة (blog)
            |       الرئيسية (index.twig)
            |       المقال (single.twig) 
            +---العلامات-التجارية (brands)
            |       الرئيسية (index.twig)
            |       العلامة (single.twig) 
            +---العميل (customer)
               |   الإشعارات (notifications.twig)
               |   الملف-الشخصي (profile.twig)
               |   المفضلة (wishlist.twig)
               +---الطلبات (orders)
               |      الرئيسية (index.twig)
               |      الطلب (single.twig)
            +---الأجزاء-الجزئية (partials)
                |   تعليق-مفرد (single-comment.twig)
                +---المنتج (product)
                  |   بطاقة-صورة-كاملة (card-full-image.twig)
                  |   بطاقة-مصغرة (card-mini.twig)
                  |   بطاقة (card.twig)
                  |   الخيارات (options.twig)
                  |   المعرض (slider.twig)
            +---المنتج (product)
            |       الرئيسية (index.twig)
            |       المنتج (single.twig)